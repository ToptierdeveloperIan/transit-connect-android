package com.example.imanicommunityapp.Network;

/**
 * Metering hint for the default network (affects optional “Wi‑Fi only” policies later).
 */
public enum NetworkCost {
    UNKNOWN,
    METERED,
    UNMETERED,
}
